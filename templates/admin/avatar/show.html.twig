<h2>{{ avatar.name }}</h2>

<div class="avatar-details">
    {% if avatar.imageUrl %}
        <img src="{{ asset(avatar.imageUrl) }}" alt="{{ avatar.name }}" width="120">
    {% else %}
            <span class="show-value">Aucune image</span>
    {% endif %}
</div>

