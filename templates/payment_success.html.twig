{% extends 'base.html.twig' %}

{% block title %}Paiement confirmé — Meurtre à Huis Clos{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/payment_success.css') }}">
{% endblock %}

{% block body %}
<h1>Meurtre à Huis Clos</h1>

<section class="success-section animate-block">
    <div class="container">

        <div class="success-icon">🔪</div>

        <h2>Paiement confirmé !</h2>
        <p class="success-subtitle">Votre enquête vous attend...</p>

        <div class="success-items">
            <h3>Récapitulatif de votre commande</h3>

            {% for item in items %}
                <div class="success-item">
                    <span class="success-item-type">
                        {% if item.type == 'pack' %}🩸{% else %}🩸{% endif %}
                    </span>
                    <span class="success-item-name">{{ item.name }}</span>
                    <span class="success-item-price">{{ item.price }} €</span>
                </div>
            {% endfor %}

            {% if promo %}
                <div class="success-item success-item-promo">
                    <span class="success-item-type">﹪</span>
                    <span class="success-item-name">
                        Code promo {{ promo.code }}
                        {% if promo.discountType == 'percentage' %}
                            (-{{ promo.discountValue }}%)
                        {% else %}
                            (-{{ promo.discountValue }} €)
                        {% endif %}
                    </span>
                    <span class="success-item-price success-item-discount">
                        -{{ promo.discount|number_format(2, ',', ' ') }} €
                    </span>
                </div>
            {% endif %}

            <div class="success-total">
                {% if promo %}
                    <div class="success-total-line">
                        <span>Sous-total</span>
                        <span>{{ total|number_format(2, ',', ' ') }} €</span>
                    </div>
                    <div class="success-total-line success-total-discount">
                        <span>Réduction</span>
                        <span>-{{ promo.discount|number_format(2, ',', ' ') }} €</span>
                    </div>
                {% endif %}
                <div class="success-total-line success-total-final">
                    <span>Total payé</span>
                    <span>{{ totalAfterDiscount|number_format(2, ',', ' ') }} €</span>
                </div>
            </div>
        </div>

        <div class="success-info">
            <p>Vos Murder Parties sont maintenant disponibles dans l'application.</p>
            <p>Téléchargez l'application pour commencer à jouer !</p>
        </div>

        <div class="success-buttons">
            <div class="download-buttons">
                <a href="https://apps.apple.com/fr/app/meurtre-%C3%A0-huis-clos/id6760702842" target="_blank">
                    <img src="{{ asset('icones/apple_darkred.png') }}" alt="Téléchargement Apple">
                </a>
                <a href="#">
                    <img src="{{ asset('icones/android_darkred.png') }}" alt="Téléchargement Android">
                </a>
            </div>
            <a href="{{ path('app_account') }}" class="btn-primary">
                Voir mes Murder Parties
            </a>
            <a href="{{ path('scenarios_list') }}" class="btn-secondary">
                Découvrir d'autres scénarios
            </a>
        </div>

    </div>
</section>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('js/scroll.js') }}"></script>
{% endblock %}